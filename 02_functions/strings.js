//strings

// declare a string type variable, naming a sentence
let sentence = 'Yet another string primitive';

//find length

// console.log(sentence.length);

//find letter at index(0) - letter stored at index 0
let letters = 'words';
// console.log(letters.charAt(0));

// sentence includes something - the word primitive
//console.log(sentence.includes("primitive"));

// convert sentence to all UPPERCASE 
console.log(sentence.toUpperCase());

// convert sentence to all LOWERCASE
console.log(sentence.toLocaleLowerCase());

/// adding 

